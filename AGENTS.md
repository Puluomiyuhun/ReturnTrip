# 接手《回程》

这是中文悬疑文字 AVG。开始修改前，先读 `docs/创作交接.md`，再通读本次涉及的 `story/` 章节。创作意图和用户已确认的取舍以交接文档为入口；各轮说明记录改动背景，不应把已经被撤回的方案重新当成要求。

- 可编辑正文与演出参数在 `story/*.md`。修改后运行 `node app/build.cjs`，同步提交生成的 `app/story.js`。不要只改生成文件。
- 执行 `node --test app/test.cjs`。文本长度或界面、声音、演出时序有变化时，按 README 运行 Electron QA，检查相应布局或时序。不要把程序检查等同于主观恐怖效果验证。
- 恐怖以细思极恐和解释失效为主，不使用巨响、突发面孔或新增谜题来填充节奏。敲击应明显弱于雨声；不要恢复之前的高增益或敲击前抽低环境声、镜头震动。
- 旁人可以见证事实，但不因此共同确认超自然。不要擅自添加异常入住须知、全知组织或另一个张明远。
- 剧本节点使用顺序编号。0.11重写开篇，采用独立存储和story ID，拒绝0.7—0.10路径；不尝试把旧节点编号映射到新剧情。后续重排须明确存档策略。
- 黑幕的剧情 scene 与最后可见的 backgroundScene 分开保存。不要把 CG 直接切到缺少素材的 black 状态，导致默认卧室闪现；详见 docs/黑幕转场修复.md。
- 将有价值的创作决策、未决问题与验证结果写入仓库 docs，供其他设备接手。临时脚本、日志和截图放 work；运行库和发行包不入库。不要提交原始网页聊天导出，其中可能带账户或会话信息。
